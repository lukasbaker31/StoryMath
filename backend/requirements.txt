fastapi>=0.104.0
uvicorn[standard]>=0.24.0
manim>=0.18.0
anthropic>=0.39.0
python-dotenv>=1.0.0
# System prerequisites (install BEFORE pip install):
#   brew install ffmpeg cairo pkg-config
# Optional (for MathTex/LaTeX support):
#   brew install --cask basictex
